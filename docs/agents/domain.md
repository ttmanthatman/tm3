# Domain docs

This repository uses a single-context domain documentation layout.

Before exploring an area, read `CONTEXT.md` at the repository root and relevant records under `docs/adr/` when they exist. Their absence is not a blocker.

Use domain terms as defined by `CONTEXT.md`. If a proposed change conflicts with an ADR, surface the conflict instead of silently overriding the decision.
